server {
    listen 80;
    server_name localhost;
	resolver 127.0.0.11 valid=10s;
    resolver_timeout 5s; 
    location /api/ {
        proxy_pass http://api:3001/;
    }
    
    location / {
        proxy_pass http://app:3000;
    }
}